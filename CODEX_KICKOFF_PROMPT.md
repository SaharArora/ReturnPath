Implement ReturnPath in this EXISTING private repository, SaharArora/testingxd. On my Mac the checkout is ~/testingxd. Do not create another repository, replace origin, change visibility, or overwrite unrelated work.

Read AGENTS.md and the entire v3 BUILD_SPEC.md, including Sections 19–21. Read docs/SETUP_MAC_AND_CLOUD.md and docs/AUTHORIZATION.md. Older handoffs/addenda are superseded. I want working code and independently checked results, not a scaffold or another design discussion.

I am using Codex desktop on my laptop. I have Gmail and Slack, but must create a Stripe account for TEST access. Make M0 usable first: project-local locked setup, external configuration loading, make auth-gmail, per-service read-only doctor, and a single concise credentials checklist. Never ask me to paste secrets into chat or print credential files. Browser account creation/consent belongs to me; continue all unblocked local implementation while I do it.

Build the narrow guarded workflow, then demonstrate actual Gmail intake/send, a Stripe TEST partial refund plus retrieval, and an actionable Slack case update in the SAME run. The LLM must actually interpret/clarify natural-language input. Neither the model API nor the local warehouse simulator counts as an external app. Do not postpone discovering real-auth blockers until the end of all fault testing.

Core experiment: a $30 authorized refund on a $100 test payment succeeds externally; kill the worker before recording completion; ingest another customer contact; restart without resetting records; discover the existing provider refund and perform no second financial operation. Grade actual provider records independently of the app's own success flag.

Preserve exact case verification, fixed amounts, durable operation identity/parameters, single-worker ownership, complete provider pagination, bounded same-key retries, UNKNOWN handling, notification uncertainty, and an independently running watchdog. The baseline watchdog detects/alerts; do not claim automatic restart. Keep runtime prompts and policy fixed. No online self-evolving policy or unsubstantiated Lyapunov/exactly-once claims.

Assume AI-assisted first-pass review for packaging, without claiming to know its platform/model. Make each important claim easy to verify: concise README and JUDGE_GUIDE.md, machine-readable submission.json, actual evaluation output, implementation/test/evidence links, genuine demo transcript/captions, truthful provenance and limitations. Generate counts from actual runs; missing connected evidence must fail submission-check rather than becoming a green badge. Do not add hidden grader instructions, self-awarded scores, or inflated claims.

Use the simple stack and milestone order. Voice is optional only after the three-service core and crash tests work. Cloud tasks, if used, remain credential-free/local-mode; do not copy Mac secrets into the repository/cloud or turn setup scripts into connected workflows.

Inspect existing changes, write a short milestone plan in IMPLEMENTATION_STATUS.md, then begin. Keep brief progress updates, small checkpoints and actual verification commands. Do not stop after planning. Finish with what ran/passed/failed/was blocked, connected-provider evidence status, exact startup steps, remaining limitations and verified private-remote/push status.
