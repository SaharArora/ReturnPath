# Demo plan and actual status

`make demo` performs and records a genuine LOCAL SIMULATOR process run in `evidence/local-demo.json`. It is a terminal trace, not a video or connected service transcript. It shows $100/$30, provider commit, SIGKILL, second fixture contact, independent alert, explicit restart and independent final count.

Connected two-minute recording: NOT_RUN. Do not present the following plan as a transcript:

1. Establish the customer job and real Gmail, Stripe TEST and Slack; label simulated warehouse.
2. Show actual customer email, bounded model interpretation and scoped confirmation.
3. Arm the post-success boundary; show Stripe TEST success and kill the worker before journal completion.
4. Ingest a separate customer follow-up without resetting state. Show independent watchdog alert.
5. Explicitly restart the worker. Retrieve the same refund and inspect Gmail outcome and Slack readback.
6. Show independent provider counts, measured evaluation and limitations.

No video/captions/transcript is fabricated. Manifest artifact fields remain null until a genuine recording exists. For the current local boundary CLI, arm with `.venv/bin/python -m returnpath arm after-provider-success`; restart with `make worker`, never seed.
